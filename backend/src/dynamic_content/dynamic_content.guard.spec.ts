import { DynamicContentGuard } from './dynamic_content.guard';

describe('DynamicContentGuard', () => {
  it('should be defined', () => {
    expect(new DynamicContentGuard()).toBeDefined();
  });
});
